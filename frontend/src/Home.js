import React, { useState } from "react";
import Sidenav from "./Sidenav";

const Home = () => {
    return (
        <div>
          <Sidenav>
          </Sidenav>
        </div>
      );


};

export default Home;
